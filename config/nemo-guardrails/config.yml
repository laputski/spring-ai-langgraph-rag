models:
  - type: main
    engine: openai
    model: llama3.2
    parameters:
      openai_api_base: "http://host.docker.internal:11434/v1"
      openai_api_key: "dummy"

rails:
  input:
    flows:
      - check input safety
  output:
    flows:
      - check output safety
